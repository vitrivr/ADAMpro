package ch.unibas.dmi.dbis.adam.main

/**
 * adamtwo
 *
 * Ivan Giangreco
 * September 2015
 */
class CLIStartup extends Runnable {
  def run() : Unit = {
    /*val settings = new Settings
    settings.usejavacp.value = true
    settings.deprecation.value = true

    new CLI().process(settings)*/
  }
}
